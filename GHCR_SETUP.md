# 🔑 Как создать GHCR_PAT (GitHub Personal Access Token)

## Шаг 1: Открой настройки GitHub

1. Иди на GitHub.com
2. Нажми на свой аватар (правый верхний угол)
3. **Settings** (Настройки)

## Шаг 2: Developer settings

1. В левом меню прокрути вниз
2. **Developer settings** (Настройки разработчика)
3. **Personal access tokens**
4. **Tokens (classic)** ← Важно! Выбери classic

## Шаг 3: Generate new token

1. **Generate new token (classic)**
2. Дай название: `Spy Game Bot GHCR`
3. Выбери срок действия: **No expiration** (или 90 days)

## Шаг 4: Выбери права (scopes)

Отметь галочками:

- ✅ `write:packages` - Upload packages to GitHub Package Registry
- ✅ `read:packages` - Download packages from GitHub Package Registry  
- ✅ `delete:packages` - Delete packages from GitHub Package Registry
- ✅ `repo` (если репозиторий приватный)

## Шаг 5: Generate token

1. Нажми **Generate token** внизу
2. **ВАЖНО:** Скопируй токен СРАЗУ! 
   - Он выглядит как: `ghp_1234567890abcdefGHIJKLmnopqrstuvwxyz`
   - После закрытия страницы увидеть его больше нельзя!

## Шаг 6: Добавь в GitHub Secrets

1. Иди в свой репозиторий `spy-game`
2. **Settings** → **Secrets and variables** → **Actions**
3. **New repository secret**
4. Name: `GHCR_PAT`
5. Secret: вставь скопированный токен
6. **Add secret**

---

## ✅ Готово!

Теперь у тебя есть все 6 секретов:

1. ✅ `SERVER_HOST` - IP сервера
2. ✅ `SERVER_USER` - root
3. ✅ `SSH_PRIVATE_KEY` - SSH ключ
4. ✅ `BOT_TOKEN` - токен бота
5. ✅ `DB_PASSWORD` - пароль БД
6. ✅ `GHCR_PAT` - токен для GHCR ← **Новый!**

---

## 🔍 Проверка

После первого деплоя проверь что образ появился:

1. Иди на главную страницу репозитория
2. Справа увидишь **Packages** 
3. Там должен быть `spy-game` образ

Или открой: `https://github.com/твой-username?tab=packages`

---

## 🐛 Troubleshooting

### "Permission denied" при пуше образа
- Убедись что GHCR_PAT имеет права `write:packages`
- Создай новый токен с правильными правами

### Образ не появляется в Packages
- Подожди 1-2 минуты после деплоя
- Проверь GitHub Actions - нет ли ошибок

### "unauthorized" на сервере
- Убедись что GHCR_PAT правильно добавлен в Secrets
- Репозиторий должен быть публичным (или токен должен иметь доступ к приватному)

---

## 💡 Преимущества GHCR

- ✅ **Быстрый деплой** - не нужно собирать образ на сервере
- ✅ **Версионирование** - каждый коммит = свой образ (по SHA)
- ✅ **Кэширование** - быстрая сборка при повторных деплоях
- ✅ **Откат** - можно вернуться к старой версии
- ✅ **Безопасность** - образы сканируются на уязвимости

Готово! 🎉
