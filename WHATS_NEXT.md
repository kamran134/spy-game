# ✅ GHCR Настроен!

## Что изменилось

Проект обновлен для использования **GitHub Container Registry (GHCR)** для хранения Docker образов.

---

## 🎯 Что теперь требуется от тебя

### 1️⃣ Создай GHCR_PAT токен

📖 **Подробная инструкция:** [GHCR_SETUP.md](GHCR_SETUP.md)

**Кратко:**
1. GitHub.com → Settings → Developer settings
2. Personal access tokens → Tokens (classic)
3. Generate new token (classic)
4. Выбери: `write:packages`, `read:packages`, `delete:packages`
5. Generate token
6. **Скопируй токен!** (будет выглядеть как `ghp_...`)

### 2️⃣ Добавь все 6 секретов в GitHub

Репозиторий → Settings → Secrets and variables → Actions

| Секрет | Значение | Где взять |
|--------|----------|-----------|
| `SERVER_HOST` | IP сервера | Твой Ubuntu сервер |
| `SERVER_USER` | `root` | Обычно root |
| `SSH_PRIVATE_KEY` | Приватный SSH ключ | `type C:\Users\kazim\.ssh\id_rsa` |
| `BOT_TOKEN` | Токен бота | @BotFather в Telegram |
| `DB_PASSWORD` | Пароль БД | Придумай любой |
| `GHCR_PAT` | GitHub токен | Только что создал ☝️ |

### 3️⃣ Запуш код на GitHub

```bash
cd c:\Users\kazim\Work\hobby\spy-game
git add .
git commit -m "Add GHCR support"
git push origin main
```

---

## 🚀 Что произойдёт автоматически

После пуша GitHub Actions:

1. ✅ **Соберет** Docker образ
2. ✅ **Запушит** образ в GHCR (ghcr.io/твой-username/spy-game)
3. ✅ **Подключится** к серверу по SSH
4. ✅ **Скачает** образ из GHCR
5. ✅ **Запустит** бот на сервере

Всё это **автоматически** за ~2-3 минуты!

---

## 📦 Где посмотреть образ

После первого деплоя:
- GitHub → твой репозиторий → справа **Packages**
- Или: `https://github.com/твой-username?tab=packages`

---

## 🎁 Преимущества GHCR

✅ **Быстрый деплой** - образ собирается 1 раз на GitHub, потом просто скачивается  
✅ **Кэширование** - следующие сборки быстрее (используют кэш слоёв)  
✅ **Версионирование** - каждый коммит = свой образ (по SHA + latest)  
✅ **Откат** - можно вернуться к любой версии  
✅ **Безопасность** - автоматическое сканирование на уязвимости  

---

## 📝 Что изменилось в файлах

### `.github/workflows/deploy.yml`
- Добавлена job `build-and-push` для сборки и пуша в GHCR
- Job `deploy` теперь зависит от `build-and-push`
- На сервере используется образ из GHCR вместо локальной сборки

### Обновлённая документация
- ✅ `GHCR_SETUP.md` - новая инструкция по настройке GHCR
- ✅ `SETUP.md` - добавлен GHCR_PAT в список секретов
- ✅ `CHECKLIST.md` - добавлена проверка GHCR_PAT
- ✅ `README.md` - упоминание GHCR в технологиях

---

## 🎯 Следующие шаги

1. **Создай GHCR_PAT** (5 минут) - [GHCR_SETUP.md](GHCR_SETUP.md)
2. **Добавь все 6 секретов** в GitHub
3. **Запуш код** - `git push origin main`
4. **Следи за деплоем** - GitHub Actions покажет прогресс
5. **Проверь бота** - отправь `/start` в Telegram

---

## ❓ Вопросы?

**Q: Можно ли сделать репозиторий приватным?**  
A: Да! Но тогда GHCR_PAT должен иметь право `repo` (полный доступ к репозиторию)

**Q: Образ будет публичным?**  
A: По умолчанию - да. Можно сделать приватным в настройках Package

**Q: Сколько места займут образы?**  
A: ~200-300 MB на версию. GitHub даёт 500 MB бесплатно для публичных репозиториев, 1 GB для Pro

**Q: Нужно ли платить?**  
A: Нет! Для публичных репозиториев GHCR бесплатен

---

## ✅ Готово!

Теперь у тебя:
- ✅ Быстрый автоматический деплой через GHCR
- ✅ Версионирование образов
- ✅ Кэширование для быстрых сборок
- ✅ Готовность к продакшн

**Удачи с деплоем!** 🚀
